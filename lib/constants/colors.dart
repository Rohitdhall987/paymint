import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0D0D0D);         // Charcoal Black
  static Color backgroundScaffold = background.withAlpha(180);
  // static const Color glassCard = Color.fromRGBO(255, 255, 255, 0.1); // Frosted Glass
  static const Color glassCard = Color(0x1AFFFFFF); // Frosted Glass
  static const Color primaryAccent = Color(0xFF00F5D4);      // Neon Cyan
  static const Color secondaryAccent = Color(0xFF9B5DE5);    // Electric Purple
  static const Color highlight = Color(0xFFFF006E);          // Dark Rose Red
  static const Color textPrimary = Color(0xFFEAEAEA);        // Light Gray
  static const Color textSecondary = Color(0xFFB0B0B0);      // Cool Gray
  static const Color borderColor = Color.fromRGBO(255, 255, 255, 0.1); // Transparent White
}
